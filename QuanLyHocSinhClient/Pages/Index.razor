@page "/"

<MudTable Items="@scores" Hover="true" Breakpoint="Breakpoint.Sm" LoadingProgressColor="Color.Info">
    <ToolBarContent>
        <MudText Typo="Typo.h6">Học kì 1 - Năm 2021-2022</MudText>
        <MudSpacer />
    </ToolBarContent>
    <HeaderContent>
        <MudTh>SemesterId</MudTh>
        <MudTh>StudentId</MudTh>
        <MudTh>TestTypeId</MudTh>
        <MudTh>SubjectId</MudTh>
    </HeaderContent>
    <RowTemplate>
        <MudTd DataLabel="SemesterId">@context.SemesterId</MudTd>
        <MudTd DataLabel="StudentId">@context.StudentId</MudTd>
        <MudTd DataLabel="TestTypeId">@context.TestTypeId</MudTd>
        <MudTd DataLabel="SubjectId">@context.SubjectId</MudTd>
    </RowTemplate>
</MudTable>

@code {
    List<Score> scores = new List<Score>();
    Score score = new Score()
    {
        SemesterId = 1,
        StudentId = new Guid("06fe4513-f2cb-43a7-8293-e34f0cc0c77b"),
        TestTypeId = 1,
        SubjectId = 2
    };
    Score score2 = new Score()
    {
        SemesterId = 1,
        StudentId = new Guid("06fe4513-f2cb-43a7-8293-e34f0cc0c77b"),
        TestTypeId = 1,
        SubjectId = 2
    };
    protected async override Task OnInitializedAsync()
    {
        await base.OnInitializedAsync();
        scores.Add(score);
        scores.Add(score2);
        var x = await API<List<Subject>>.QLDiem.GetAsync("/api/Subjects");
    }
}